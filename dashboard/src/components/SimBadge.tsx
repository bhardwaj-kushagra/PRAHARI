/** Permanent SIMULATION badge (CLAUDE.md rule 15, SPEC §6.2). */
export function SimBadge() {
  return (
    <span className="sim-badge" role="status" aria-label="All values shown are simulation output">
      SIMULATION
    </span>
  );
}
